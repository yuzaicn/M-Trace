# 贡献指南

## 洁净室规则（**最重要的一条**）

本项目的实现必须与任何同类的既有实现**完全没有代码、prompt、文案或数据上的血缘关系**。这不是风格偏好，是许可合规的前提。

- **读过任何同类参考实现的人，不写实现代码。** 只产出"行为规格"（输入什么、观察到什么、判据是什么），不产出代码结构、函数名、系数取值或 prompt 原文。
- **写实现的人，不读同类参考实现。**
- 规格与实现分离，并留存书面记录。

**禁止**：

- 复制、翻译、改写、重排任何外部项目的代码（任何语言、任何文件）；
- 逐字或近逐字复刻其 prompt 模板、README、UI 文案；
- 取用其指纹库数值、校准参数或数据结构；
- 在代码、注释、文档或 commit message 里留下任何出处标注、灵感来源说明，或暗示源自他人作品的表述。
- 删除、替换或迁移任何第三方的版权声明或许可文本。

## 每个 PR 必须自检

- [ ] 本 PR 未引用、未改写、未翻译任何第三方项目的代码、prompt、文案或数据。
- [ ] 未引入运行时依赖（`dependencies` 必须为空；`devDependencies` 不受限）。
- [ ] 未删改他人的版权声明或许可文本。

## 提交前必须做的

```bash
npm run gate:full
```

本仓库**没有** GitHub Actions 门禁 —— 唯一的自动化回归信号就是这个脚本。请把它输出的证据块原样粘进 PR 描述。

门禁包含：类型检查、格式检查、lint、构建、契约测试。数据到位后还会包含夹具回归。

## 契约变更流程

`src/types/` 与 `docs/interface.md` 是**冻结**的。要改：

1. 在对应 issue 上写明：要改的签名、原因、影响的下游模块；
2. 通知全部受影响的实现与验收人；
3. 更新 `docs/interface.md`（版本号 +1）、受影响的类型、以及对应的契约测试；
4. 变更未通知下游就合并 → 回退。

**契约里的每个条款都必须在 `test/fixtures/contract-intents.json` 登记一条 intent**（条款 → 测试的对照）。新增条款而不登记 intent，等于新增了一条没人守的规则。

## 阈值不是代码

判据阈值（距离上限、边际下限、可信度区间……）**只能来自参考库的 `calibration` 字段**，代码里出现字面量即为违约。有一条契约测试会扫源码断言这一点。

要调阈值：重新校准 → 更新库 → 在 `docs/calibration.md` 留下版本条目与依据。改代码里的数字不是"调参"，是绕过流程。

## 失败时怎么办

- 夹具里 `expected.decision` 为 `unknown` 的用例被判成 `in-library` → **门禁失败，无论总体准确率多高**。这是本项目的零容忍项，不要靠调阈值让它变绿。
- 门禁红了就修实现，**不要**用 `skip`、删除测试、注释断言或加 build tag 的方式让它变绿。
