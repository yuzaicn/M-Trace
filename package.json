{
  "name": "m-trace",
  "version": "0.0.0",
  "description": "Trace and verify the model identity behind AI coding interactions.",
  "type": "module",
  "bin": {
    "m-trace": "./bin/m-trace.js"
  },
  "scripts": {
    "build": "node --check bin/m-trace.js",
    "lint": "eslint . && prettier --check .",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "devDependencies": {
    "eslint": "^9.35.0",
    "prettier": "^3.6.2"
  }
}
